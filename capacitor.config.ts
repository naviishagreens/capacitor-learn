import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.naviishagreens.capapp',
  appName: 'capacitor-learn',
  webDir: 'dist/capacitor-learn',
  bundledWebRuntime: false
};

export default config;
